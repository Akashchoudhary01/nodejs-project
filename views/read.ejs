<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="stylesheet/style1.css">
    <!-- Tailwind css -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- external css -->
    <link rel="stylesheet" href="/stylesheet/style2.css">
</head>

<body>
    <div class=" main w-full h-full bg-zinc-900 p-10 text-white">
        <div class=" link mb-10">
            <a href="/" class=" a text-blue-500">back To Home Page</a>
        </div>
        <h1 class="text-3xl tracking-tighter mb-5 , ">All Users</h1>

        <div class="users flex gap-2 flex-wrap">
            <% if(users.length >0) {%>
                <% users.forEach(function(user){ %>
                    <div class="user w-72 p-4 bg-zinc-800 rounder-lg">
                        <div class=" container w-full h-72 rounded-lg bg-red-500">
                            <img class=" img w-full h-full object-cover overflow-hidden object-top" src="<%= user.image%>" alt="">
                        </div>
                        <h3 class="h3 text-xl tracking-tighter mt-3 ">
                            <%= user.name%>
                        </h3>
                        <h5 class=" h5 text-zinc-500">
                            <%= user.email %>
                        </h5>
                        <div class="flex linkss gap-3 mt-10 ">
                            <a href="/edit/<%= user._id %>" class=" edit text-zinc-200">Edit User</a>
                            <a href="/delete/<%= user._id %>" class=" delete text-red-200">Delete User </a>
                        </div>
                    </div>
                    <% }) %>
                        <%}else{%>
                            <h2 class=" backup-text text-zinc-500">No User Yet</h2>
                            <% }%>

        </div>


</body>

</html>